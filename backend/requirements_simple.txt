# Core FastAPI packages (pre-compiled wheels)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database & Storage (pre-compiled versions)
supabase==2.3.0
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.0
requests==2.31.0
aiofiles==23.2.1

# Simple ML/Data packages (pre-compiled)
# Note: pandas, numpy, scikit-learn will need to be installed separately
# if compilation tools are available
